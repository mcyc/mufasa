version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip  # Use setup.py to install package dependencies
    - requirements: docs/requirements.txt  # Additional dependencies for documentation
