version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip  # Install your package using setup.py
    - requirements: docs/requirements.txt  # Install additional dependencies for docs
