:html_theme.sidebar_secondary.remove:

.. _api_ref:

=============
API Reference
=============

This is the class and function reference of **MUFASA**. In addition to the table below,
you can also use the search bar on the top right side of the webpage.

.. toctree::
   :maxdepth: 2
   :hidden:

   {% for module in API_REFERENCE -%}
   {% if module.startswith('mufasa.')
         and module.count('.') == 1
         and '._' not in module -%}
   {{ module }}
   {% endif -%}
   {% endfor %}

.. list-table::
   :header-rows: 1
   :class: apisearch-table

   * - Object
     - Description
   {% for module, module_info in API_REFERENCE.items() -%}
   {% for obj in module_info["sections"][0]["autosummary"] -%}
   {% if module_info["type"].get(obj) in ["class", "function"] -%}
   * - :obj:`~{{ module }}.{{ obj }}`
     - .. container:: sk-apisearch-desc

          {{ module_info["sections"][0]["descriptions"].get(obj, "No description available.") }}

          .. container:: caption

             :mod:`{{ module }}`
   {% endif -%}
   {% endfor %}
   {% endfor %}
